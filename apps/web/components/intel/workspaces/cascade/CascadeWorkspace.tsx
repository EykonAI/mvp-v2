'use client';
import ComingSoonPanel from '@/components/intel/shell/ComingSoonPanel';

export default function CascadeWorkspace() {
  return (
    <ComingSoonPanel
      phase="Phase 4 · Scenario workspaces"
      description="Infrastructure Cascade Map: seed-node selector (refineries/pipelines/grid/ports/LNG) + capacity-loss slider + 4-tier DAG visualisation (Source → Transit → Processing → Delivery) + substitution-cost table + grid-stress indicator. Scaffold in Phase 1; max-flow reroute in Phase 4."
    />
  );
}
