'use client';
import ComingSoonPanel from '@/components/intel/shell/ComingSoonPanel';

export default function SanctionsWorkspace() {
  return (
    <ComingSoonPanel
      phase="Phase 4 · Scenario workspaces"
      description="Sanctions Expansion Wargame: OFAC/EU/UK OFSI/UN/G7 toggle + sanction presets + target entities autocomplete + propagation depth + force-directed network graph + affected-fleet scope + flag-hop destination bar chart. Scaffold in Phase 1; propagation + graph in Phase 4."
    />
  );
}
