'use client';
import ComingSoonPanel from '@/components/intel/shell/ComingSoonPanel';

export default function ShadowFleetWorkspace() {
  return (
    <ComingSoonPanel
      phase="Phase 5 · Explorer workspaces"
      description="Shadow Fleet Profiler. Leads list (oil/LNG/grain commodity toggle + MMSI/IMO search) · Evidence viewer (AIS track, flag/owner history, port calls, ownership graph, Sentinel imagery) · Contributing indicators ledger + cluster membership · Evidence-pack export. Lands in Phase 5."
    />
  );
}
