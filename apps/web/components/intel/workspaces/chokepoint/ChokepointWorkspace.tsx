'use client';
import ComingSoonPanel from '@/components/intel/shell/ComingSoonPanel';

export default function ChokepointWorkspace() {
  return (
    <ComingSoonPanel
      phase="Phase 4 · Scenario workspaces"
      description="Closure & reroute model with three-column scenario builder (chokepoint selector + closure type + duration), price-envelope chart, diverted-vessel map, refining impact bars, and Claude-written consequence summary. Scaffold renders in Phase 1; interactive logic lands in Phase 4."
    />
  );
}
